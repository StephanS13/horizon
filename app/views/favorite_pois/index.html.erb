<div class="container-favorite-pois">

  <div class="favorite-pois-infos">
    <h2>Mes coups de coeur à <%= @city.name %></h2>
    <div class="back-to-profile-button">
      <%= link_to "Retour au profil", profile_path, class: "btn-primary" %>
    </div>
  </div>

  <div class="map">
      <div  id="map"
            style="width: 100%; height: 600px"
            data-markers="<%= @markers.to_json %>"
            data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"></div>
  </div>
</div>
